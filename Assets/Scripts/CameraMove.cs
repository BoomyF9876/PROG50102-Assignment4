using UnityEngine;

[DefaultExecutionOrder(2)]
public class CameraMove : MonoBehaviour
{
    void Awake()
    {
        Debug.Log("3. Camera Awake");
    }

    void Start()
    {
        Debug.Log("3. Camera Start");
    }

    // Update is called once per frame
    void Update()
    {
        Debug.Log("3. Camera Update");
    }
}
